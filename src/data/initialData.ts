// ── Global constants ──────────────────────────────────────────────────────────
export const MAX_PLAYERS = 8;
export const MAX_STRIKES = 4;

// ── Initial state: empty arrays (data will come from the API / user input) ─────
export const INITIAL_PLAYERS: never[] = [];
export const INITIAL_NPCS:    never[] = [];
